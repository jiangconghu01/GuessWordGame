# 猜词游戏（GuessWordGame）设计文档

## 1. 项目概述

### 1.1 背景
一款纯前端中文语义猜词游戏。系统每轮给出一个目标词汇，玩家可输入任意中文词语或单字，页面实时显示该输入与目标词汇的语义关联度（0%–100%），玩家根据关联度提示不断调整猜测，直至命中目标词。

### 1.2 目标
- 零后端依赖，浏览器内即可运行
- 支持用户**任意中文输入**都能给出关联度反馈
- 游戏节奏轻快，单局时长 1–5 分钟
- 界面简洁直观，支持历史猜测记录与冷热提示

---

## 2. 核心玩法

### 2.1 游戏流程
1. **开局**：系统从词库中随机抽取一个目标词（不展示给玩家）。
2. **猜测**：玩家在输入框中输入任意中文词汇或单字，点击"猜测"或按回车。
3. **反馈**：页面显示该输入与目标词的语义关联度（百分比），同时将本次猜测加入历史列表。
4. **迭代**：玩家根据关联度高低调整下一次输入，关联度越高表示越接近。
5. **结算**：当玩家输入与目标词完全一致（或相似度达到 99.5%+）时，判定为猜中，展示结果与统计信息。

### 2.2 胜负与评分
- **胜利条件**：输入文本与目标词完全一致（不区分简繁，去除标点）。
- **评分维度**：
  - 猜测次数（越少越好）
  - 耗时（越短越好）
  - 可展示历史最佳记录

### 2.3 辅助功能
- **历史记录面板**：按关联度从高到低排序，视觉冷热区分（高关联度暖色、低关联度冷色）。
- **提示功能**：每轮前 3 次猜测后，可点击"给点提示"解锁目标词的词性或类别标签（如"名词"、"抽象概念"）。
- **换词**：玩家可随时放弃当前词，切换新词。

---

## 3. 技术架构

### 3.1 技术栈
- **框架**：原生 HTML5 + TypeScript + Vite（构建工具）
- **样式**：原生 CSS3（CSS Variables + Flex/Grid），无需 UI 框架以控制体积
- **数据**：静态 JSON 资源（词库、字符向量表、高频词向量表）
- **可选增强**：`transformers.js`（ONNX Runtime Web）用于加载轻量中文句子编码器

### 3.2 目录结构（预期）
```
GuessWordGame/
├── index.html
├── src/
│   ├── main.ts
│   ├── game/
│   │   ├── GameEngine.ts      # 游戏状态与流程控制
│   │   ├── WordBank.ts        # 词库管理
│   │   └── Scoring.ts         # 计分逻辑
│   ├── similarity/
│   │   ├── SimilarityEngine.ts    # 相似度计算调度器
│   │   ├── CharLevelEmbedder.ts   # 字符级向量编码
│   │   ├── HighFreqEmbedder.ts    # 高频词精确向量
│   │   └── TransformerEmbedder.ts # transformers.js 增强（可选）
│   ├── ui/
│   │   ├── App.ts
│   │   ├── GuessInput.ts
│   │   ├── GuessHistory.ts
│   │   └── ResultPanel.ts
│   └── utils/
│       ├── textNormalize.ts   # 文本清洗、分字
│       └── cosineSimilarity.ts
├── public/
│   ├── word-bank.json         # 目标词库
│   ├── char-vectors.json      # 字符向量表
│   └── highfreq-vectors.json  # 高频词精确向量
└── docs/
    └── design.md
```

---

## 4. 语义相似度计算方案（混合架构）

### 4.1 设计原则
纯前端覆盖任意中文输入，必须采用**多级 Fallback** 策略，在精度、体积、速度之间取得平衡。

### 4.2 三级 Fallback 链

```
用户输入 → 文本清洗
    │
    ▼
[Level 1] 高频词精确匹配
    - 输入词是否在高频词向量表（Top 20,000 词）中？
    - 是 → 直接使用预训练的精确词向量（最高精度）
    - 否 → 继续
    │
    ▼
[Level 2] Transformers.js 模型增强（可选/异步）
    - 若用户设备性能足够且模型已缓存/加载
    - 使用轻量中文句子编码器（如 bge-small-zh-v1.5 ONNX 导出）
    - 将输入编码为 384/512 维向量
    - 若模型返回有效向量 → 计算余弦相似度
    - 若模型未就绪或不认识该词 → 继续
    │
    ▼
[Level 3] 字符级语义向量（保底）
    - 将输入拆分为单个汉字
    - 查字符向量表（约 8,000 常用字 × 128 维）
    - 对字向量做加权平均（TF-IDF 权重或均匀平均）
    - 得到输入的语义向量
    - 与目标词向量计算余弦相似度
    │
    ▼
[Level 4] 完全未知处理
    - 输入包含大量生僻字/符号（字符表中覆盖率 < 50%）
    - 返回 0% 并提示"无法识别输入"
```

### 4.3 数据资源规格

| 资源 | 格式 | 规模 | 体积（量化后） | 加载时机 |
|------|------|------|----------------|----------|
| 字符向量表 | JSON/Binary | 8,000 字 × 128 维 | ~2–3 MB | 游戏初始化时同步加载 |
| 高频词向量表 | JSON/Binary | 20,000 词 × 128 维 | ~5–7 MB | 游戏初始化时同步加载 |
| 词库 | JSON | 1,000–5,000 目标词 | ~50–200 KB | 游戏初始化时同步加载 |
| ONNX 模型（可选） | `.onnx` | bge-small-zh 或更轻量 | ~30–50 MB | 首次游戏后后台异步加载，二次访问生效 |

### 4.4 相似度映射
- **原始值**：余弦相似度 `cos(θ)` ∈ [-1, 1]
- **游戏映射**：`score = max(0, cos(θ)) × 100%`
  - 语义无关的词在向量空间中通常接近正交（cos ≈ 0），故直接截断到 0%
  - 完全同义词可达 95–100%
- **目标词匹配**：当输入经文本归一化后与目标词完全一致，直接判定胜利（不依赖相似度阈值，避免"开心"与"高兴"因向量相近而误触发）

### 4.5 文本归一化规则
1. 转为小写（英文部分）
2. 去除首尾空白、标点符号
3. 繁体转简体（使用开源映射表，约 2KB）
4. 去除重复字（如"开开开开心" → "开心"）仅用于匹配，不用于相似度计算

---

## 5. 词库设计

### 5.1 目标词库
- **规模**：首发 1,000 词，后续可扩展至 5,000 词
- **选词原则**：
  - 常用中文词汇（现代汉语常用词表）
  - 覆盖名词、动词、形容词、抽象概念
  - 避免过于生僻、专有名词、敏感词
  - 单字词占比 10%，双字词 60%，三字及以上 30%
- **分类标签**：每个词附带类别标签（如"自然"、"情感"、"物品"、"动作"），用于提示功能

### 5.2 词库格式示例
```json
[
  { "word": "情绪", "category": "情感", "difficulty": 3 },
  { "word": "桌子", "category": "物品", "difficulty": 1 }
]
```

---

## 6. UI/UX 设计

### 6.1 页面布局（单页应用）

```
┌──────────────────────────────────────┐
│  GuessWordGame          [统计] [设置]  │  ← Header
├──────────────────────────────────────┤
│                                      │
│         第 N 轮                        │  ← 轮次/关卡信息
│      [目标词] ???                      │  ← 目标词占位（猜中后显示）
│                                      │
│   ┌────────────────────────────┐     │
│   │  输入你的猜测...            │     │  ← 输入框
│   └────────────────────────────┘     │
│           [ 猜 测 ]                  │  ← 提交按钮
│                                      │
│   关联度: [████░░░░░░] 45.2%         │  ← 最近一次反馈（进度条+数值）
│                                      │
│   ┌── 猜测历史 ──────────────────┐   │
│   │ 🔥 心情    92.5%             │   │
│   │ 🌡️ 感觉    78.3%             │   │  ← 历史列表（按关联度降序）
│   │ ❄️ 天气    12.1%             │   │
│   │ 🧊 金属     0.0%             │   │
│   └──────────────────────────────┘   │
│                                      │
│   [给点提示]  [换一个新词]            │  ← 辅助按钮
│                                      │
└──────────────────────────────────────┘
```

### 6.2 视觉设计
- **主题**：浅色模式为主（后期可扩展暗色模式）
- **色彩**：
  - 高关联度（>80%）：暖色调（橙/红渐变）
  - 中关联度（30–80%）：中性色调（黄/绿）
  - 低关联度（<30%）：冷色调（蓝/青）
- **字体**：系统默认无衬线字体（`system-ui, -apple-system, sans-serif`），中文回退 "PingFang SC", "Microsoft YaHei"
- **动效**：猜测提交后关联度数字滚动动画、历史列表平滑插入、猜中后的庆祝动效（CSS confetti）

### 6.3 交互细节
- 输入框支持回车键快速提交
- 历史列表自动滚动到顶部（最新高关联度猜测）
- 输入与历史去重：同一词汇重复输入时提示"已猜过，关联度 XX%"
- 移动端适配：输入框固定底部，历史列表可滚动

---

## 7. 状态管理与数据流

### 7.1 游戏状态机
```
[LOADING] ──加载完成──→ [IDLE]
  │                        │
  │                        ▼ 点击开始/换词
  │                     [PLAYING]
  │                        │
  │         提交猜测 ──────┼──────→ 计算相似度
  │                        │         │
  │                        ▼         │
  │                     [GUESSING] ◀─┘
  │                        │
  │         猜中 ──────────┼─────────→ [WON]
  │         放弃/换词 ─────┘             │
  │                                      │
  └──────────────────────────────────────┘
```

### 7.2 核心状态（GameState）
```typescript
interface GameState {
  status: 'loading' | 'idle' | 'playing' | 'guessing' | 'won';
  targetWord: string;           // 当前目标词
  guesses: GuessRecord[];       // 猜测历史
  guessCount: number;           // 本局猜测次数
  startTime: number;            // 本局开始时间戳
  bestScore: number;            // 当前最高关联度（用于进度提示）
  hintUsed: boolean;            // 是否已使用提示
}

interface GuessRecord {
  word: string;
  similarity: number;           // 0.0 – 1.0
  rank: number;                 // 在所有猜测中的排名（1 表示最接近）
  timestamp: number;
}
```

### 7.3 本地存储（localStorage）
- 玩家历史最佳记录（最少步数、最短耗时）
- 已猜中词汇列表（避免短期内重复出现）
- 可选：ONNX 模型缓存标记（避免重复下载）
- 设置项（主题、音效开关等）

---

## 8. 性能要求

| 指标 | 目标 |
|------|------|
| 首屏加载时间 | < 2s（3G 网络下字符向量表+词库加载完成） |
| 相似度计算延迟 | < 100ms（Level 1/3），< 500ms（Level 2 模型推理）|
| 交互响应 | 输入提交到 UI 更新 < 16ms（动画流畅 60fps）|
| 总资源体积 | 首屏必须资源 < 10MB，可选模型 < 50MB |
| 兼容性 | Chrome 90+, Edge 90+, Firefox 88+, Safari 14+ |

---

## 9. 部署方案

- **托管**：纯静态资源，可直接部署到 GitHub Pages / Vercel / Netlify / Cloudflare Pages
- **构建**：Vite 生产构建，启用 gzip/brotli 压缩
- **CI/CD**：GitHub Actions 自动构建并推送到 Pages
- **域名**：默认使用 GitHub Pages 子域名，或绑定自定义域名

---

## 10. 里程碑

| 阶段 | 内容 | 预估工期 |
|------|------|----------|
| M1 | 项目脚手架 + 核心 UI 布局 + 词库加载 | 1 天 |
| M2 | 字符级语义向量 + 高频词表 + 相似度计算 | 1 天 |
| M3 | 游戏状态机 + 猜测流程 + 历史面板 + 冷热提示 | 1 天 |
| M4 | 提示功能 + 本地存储 + 统计面板 + 响应式适配 | 1 天 |
| M5 | 动效打磨 + 性能优化 + 多浏览器测试 | 1 天 |
| M6 | Transformers.js 增强（可选）| 1–2 天 |

---

## 11. 风险与回退策略

| 风险 | 影响 | 回退策略 |
|------|------|----------|
| 字符级向量精度不足，游戏提示失效 | 高 | 扩充高频词精确向量表至 5 万词；或切换为更大维度字向量（128→256） |
| 资源加载过慢（网络差） | 中 | 支持 Service Worker 离线缓存；提供加载进度条与 retry 机制 |
| 浏览器不支持 WebGL/ONNX | 低 | 优雅降级：禁用 Level 2 模型，仅使用 Level 1/3 |
| 敏感词/不当目标词 | 中 | 词库人工审核 + 敏感词过滤列表；玩家可举报 |
| 繁体/异体字匹配失败 | 低 | 文本归一化层强制转简体；维护繁简映射表 |

---

## 12. 附录

### 12.1 技术选型理由
- **为什么不用 React/Vue**：控制包体积，避免引入 30KB+ 运行时；此项目交互逻辑简单，原生 TS 足够。
- **为什么用 TypeScript**：类型安全有助于维护向量运算接口和游戏状态机。
- **为什么字符向量是保底**：中文字符集有限（常用 < 1 万字），任意词可拆解为字；字向量平均虽粗但稳定，且体积极小。

### 12.2 相似度计算示例
目标词："情绪"  
用户输入："金"  
→ "情绪" 拆字向量平均 = Vec(情) + Vec(绪)  
→ "金" 查字向量 = Vec(金)  
→ cos(θ) ≈ 0.02 → 映射为 2.0% ≈ **0%**（截断后）  

目标词："情绪"  
用户输入："心情"  
→ cos(θ) ≈ 0.82 → 映射为 **82.0%**
